server:
  port: 8888
security:
  basic:
    enabled: false
management:
  security:
    enabled: false
spring:
  cloud:
    config:
      server:
        git:
          uri: git@github.com:choipin/matcher.git
#          ignore-local-ssh-settings: true

#          skip-ssl-validation: true